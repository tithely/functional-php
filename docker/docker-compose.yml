services:
    php_84:
        build:
            dockerfile: docker/84.Dockerfile
            context: ../
        container_name: functional_php_84
        environment:
          - PCOV_ENABLED=1
          - PCOV_DIRECTORY=/app/src
        volumes:
            - ../:/app
